LabManager
==========

The LabManager object manages connections to multiple InstrumentManagers on different hosts and simplifies
interactions with instruments regardless of where they are located. It simplifies script development by abstracting
instruments as Python objects.

.. autoclass:: labtronyx.LabManager
   :members:
